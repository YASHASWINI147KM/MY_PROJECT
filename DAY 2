#include <stdio.h>
int main()
{
char name[100];
int age;
printf("Enter the name: ");
scanf("%s", &name);
printf("Enter Age : ");
scanf("%d", &age);

printf("\nName is %s",name);

printf("\nage is %d",age);

    return 0;
}
