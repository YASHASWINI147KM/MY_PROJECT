#include <stdio.h>
int main() {
    float x,y,result;
    printf("enter the first number:");
    scanf("%f",&x);
    printf("enter the second number:");
    scanf("%f",&y);
    result=x+y;
    printf("\nresult is %f", result);
    return 0;
}

#include <stdio.h>
int main() {
    float x,y,result;
    printf("enter the first number:");
    scanf("%f",&x);
    printf("enter the second number:");
    scanf("%f",&y);
    result=x-y;
    printf("\nresult is %f", result);
    return 0;
}

#include <stdio.h>
int main() {
    float x,y,result;
    printf("enter the first number:");
    scanf("%f",&x);
    printf("enter the second number:");
    scanf("%f",&y);
    result=x*y;
    printf("\nresult is %f", result);
    return 0;
}

#include <stdio.h>
int main() {
    float x,y,result;
    printf("enter the first number:");
    scanf("%f",&x);
    printf("enter the second number:");
    scanf("%f",&y);
    result=x/y;
    printf("\nresult is %f", result);
    return 0;
}

#include <stdio.h>
int main() {
    float x,y,result;
    printf("enter the first number:");
    scanf("%f",&x);
    printf("enter the second number:");
    scanf("%f",&y);
    result=x%y;
    printf("\nresult is %f", result);
    return 0;
}
